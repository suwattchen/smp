# File: apps/frontend/.env.development
# ตั้งค่าเพื่อให้ Next.js รู้ว่า API อยู่ที่ไหน

# Host: ใช้ชื่อ Service 'kong' เพราะ Frontend อยู่ใน Network เดียวกัน
# Port: ใช้พอร์ต 8000 ซึ่งเป็น Proxy Port ของ Kong
# Path: คุณอาจจะต้องเพิ่ม Path ที่ Kong ใช้ในการ Route ไปยัง Core-Go
# (โดยทั่วไปอาจเป็น /api หรือ /core-go ขึ้นอยู่กับการตั้งค่าใน infra/kong/kong.yml)

# เราจะตั้งค่าแบบสมมติว่า Kong มี Route /api ที่ชี้ไปที่ Core-Go
NEXT_PUBLIC_API_URL=http://kong:8000/api
